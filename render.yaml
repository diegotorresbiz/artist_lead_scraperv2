
services:
  - type: web
    name: outreach-agent-scraper
    env: docker
    dockerfilePath: ./Dockerfile.render
    plan: starter
    region: oregon
    buildCommand: ""
    startCommand: ""
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
      - key: ENABLE_WEB_SCRAPING
        value: "true"
      - key: PORT
        value: "8000"
    autoDeploy: false
    disk:
      name: chrome-cache
      mountPath: /tmp
      sizeGB: 1
